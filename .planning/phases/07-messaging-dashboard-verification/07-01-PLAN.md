---
phase: 7
phase_name: Messaging & Dashboard Trust Signals Verification
plan_number: "07-01"
title: Create Phase 4 Verification Artifact for Identity Fallback
depends_on: []
files_modified:
  - .planning/phases/04-messaging-dashboard/VERIFICATION-REPORT.md
priority: high
estimated_hours: 2
---

# Phase 7.1: Create Phase 4 Verification Artifact (Identity Fallback)

## Objective

Create Phase 4 verification artifact documenting messaging identity fallback and dashboard rating signal requirements (MSGS-01, MSGS-02, DASH-01, DASH-02) with comprehensive UAT validation.

## Problem Analysis

**Current State:**
- Phase 4 requirements are implemented in components but no verification artifact exists
- Messaging identity fallback logic exists in ConversationList and ChatHeader components
- Dashboard rating display logic exists in RatingCard and RatingBreakdown components
- Audit flags all 4 requirements as "partial" due to missing verification evidence

**Audit Finding:**
```
MSGS-01, MSGS-02, DASH-01, DASH-02: status: partial
Evidence: "Code exists; no phase 4 verification artifacts are present."
```

## Success Criteria

- ✅ VERIFICATION-REPORT.md created in `.planning/phases/04-messaging-dashboard/`
- ✅ All 4 requirements documented with test results
- ✅ MSGS-01: Conversation list shows stable identity labels (no generic placeholders)
- ✅ MSGS-02: Chat header shows consistent counterpart identity (no unnamed participants)
- ✅ DASH-01: Rating card displays normalized average with correct formatting
- ✅ DASH-02: Rating card click reveals 5★ through 1★ distribution
- ✅ All test cases pass (unit + integration + UAT)
- ✅ Report includes sign-off and acceptance status

## Tasks

<task id="7.1.1">
**Research implementation: Messaging identity fallback (MSGS-01, MSGS-02)**

1. Read code:
   - `src/pages/Messages.tsx` or `src/components/Messaging/*`
   - `src/components/ConversationList.tsx` — identity display logic
   - `src/components/ChatHeader.tsx` — participant name/label logic

2. Identify:
   - How participant identity is resolved (user profile lookup, cache, fallback)
   - What fallback label is used when profile unavailable (e.g., "Pro User", "Resident")
   - Edge cases: deleted account, missing email, blocked user, etc.
   - Current test coverage for identity rendering

3. Extract:
   - Fallback logic flow (step-by-step)
   - Data fetching pattern (real-time, cached, hydrated)
   - Component props and state relevant to identity display

4. Document in draft VERIFICATION-REPORT.md

**Acceptance:**
- Identity fallback logic understood
- Implementation locations identified
- Edge cases documented
</task>

<task id="7.1.2">
**Research implementation: Dashboard rating display (DASH-01, DASH-02)**

1. Read code:
   - `src/components/Dashboard/RatingCard.tsx` — average rating display
   - `src/components/Dashboard/RatingBreakdown.tsx` or modal — star distribution
   - `src/utils/ratingUtils.ts` or similar — normalization logic

2. Identify:
   - How average rating is calculated (mean, weighted, etc.)
   - Formatting: decimal places, rounding behavior, display format
   - Star rating breakdown: how 5-star distribution is stored and displayed
   - Click handler: what triggers breakdown popup
   - Current test coverage

3. Extract:
   - Normalization formula (e.g., rounding to 1 decimal)
   - Display format (e.g., "4.5★" or "4.5 out of 5")
   - Breakdown data structure and rendering logic

4. Document in draft VERIFICATION-REPORT.md

**Acceptance:**
- Rating display logic understood
- Implementation locations identified
- Normalization approach documented
</task>

<task id="7.1.3">
**Create VERIFICATION-REPORT.md structure**

1. Create file: `.planning/phases/04-messaging-dashboard/VERIFICATION-REPORT.md`
2. Use template:
   ```markdown
   ---
   phase: 4
   phase_name: Messaging and Dashboard Trust Signals
   verified_on: 2026-04-04
   requirements:
     - MSGS-01
     - MSGS-02
     - DASH-01
     - DASH-02
   verification_status: in_progress
   qa_sign_off: null
   ---
   
   # Phase 4 Verification Report
   
   ## Requirement: MSGS-01
   ```

3. Add sections for each requirement with placeholders for:
   - Test cases
   - Results (pass/fail)
   - Edge cases
   - Notes

**Acceptance:**
- Report structure created with all 4 requirements
- Ready for UAT results to be filled in
</task>

<task id="7.1.4">
**Unit tests: Messaging identity fallback (MSGS-01)**

1. Create test: `src/components/__tests__/ConversationList.test.tsx`
2. Test cases:
   - Identity label displays correctly when profile is hydrated
   - Fallback label (e.g., "Pro User") displays when profile unavailable
   - Multiple conversations show correct identity for each participant
   - Deleted account shows appropriate fallback (not "undefined" or blank)
   - Blocked user shows appropriate label if different from normal fallback

3. Mock data patterns:
   - Conversation with full profile
   - Conversation without profile (fallback needed)
   - Mixed scenarios

4. Assert:
   - No generic "User" or "Unknown" labels (specificity required)
   - Fallback is consistent across all conversations

**Acceptance:**
- 4+ test cases pass
- Coverage >80% for identity rendering logic
- No generic labels in output
</task>

<task id="7.1.5">
**Unit tests: Chat header identity (MSGS-02)**

1. Create test: `src/components/__tests__/ChatHeader.test.tsx`
2. Test cases:
   - Chat header displays counterpart identity correctly when profile available
   - Fallback label displays when profile unavailable (same as MSGS-01)
   - Identity remains stable across re-renders
   - Deleted/blocked user shows appropriate treatment

3. Mock data:
   - Full partner profile
   - Missing partner profile
   - Deleted partner account

4. Assert:
   - Header title never shows generic "Unknown" or blank
   - Label is user-friendly and informative

**Acceptance:**
- 4+ test cases pass
- Coverage >80% for identity display in header
- No generic labels
</task>

<task id="7.1.6">
**Unit tests: Rating normalization and display (DASH-01)**

1. Create test: `src/components/__tests__/RatingCard.test.tsx`
2. Test cases:
   - Average rating displays with correct formatting (e.g., 4.5)
   - Rounding behavior is consistent (e.g., 4.45 → 4.5, not 4.4 or 4.9)
   - Edge case: no ratings → displays "No ratings" or similar
   - Edge case: single rating → displays correctly
   - Edge case: many ratings → normalization is accurate

3. Mock rating data:
   - Full 5-star profile (avg 4.8)
   - Poor ratings (avg 2.1)
   - Mixed ratings (avg 3.5)

4. Assert:
   - Formatting is consistent with spec
   - Rounding matches specification

**Acceptance:**
- 5+ test cases pass
- Coverage >80% for rating display logic
- Rounding behavior validated
</task>

<task id="7.1.7">
**Unit tests: Star breakdown popup (DASH-02)**

1. Create test: `src/components/__tests__/RatingBreakdown.test.tsx`
2. Test cases:
   - Clicking rating card opens popup/modal
   - Popup displays 5-star through 1-star breakdown
   - Each star row shows count and percentage
   - Percentages sum to 100%
   - Closing popup works correctly

3. Mock breakdown data:
   ```
   5★: 80 (40%)
   4★: 60 (30%)
   3★: 40 (20%)
   2★: 15 (7.5%)
   1★: 5 (2.5%)
   Total: 200
   ```

4. Assert:
   - Breakdown chart is accurate
   - Percentages calculated correctly
   - UI is clear and accessible

**Acceptance:**
- 5+ test cases pass
- Coverage >80% for breakdown display
- Math is correct
</task>

<task id="7.1.8">
**Integration test: Messaging to Dashboard flow**

1. Create test: `src/pages/__tests__/Dashboard.integration.test.tsx`
2. Test scenario:
   - User navigates to conversations
   - Identity labels display correctly
   - User opens conversation (header shows correct identity)
   - User navigates to dashboard
   - Rating card displays with average
   - Click rating card → breakdown shows 5-star distribution
   - Close breakdown → rating card visible again

3. Mock services:
   - Firestore queries return realistic conversation + rating data
   - Identity resolution works end-to-end

**Acceptance:**
- Integration test passes
- Full flow from messaging to dashboard works
- No missing identities or ratings
</task>

<task id="7.1.9">
**Execute UAT: Messaging identity fallback (MSGS-01, MSGS-02)**

1. In staging environment:
   - Open conversations list
   - For each conversation, verify:
     - ✅ Participant name/label is shown (not blank)
     - ✅ If profile available: real name shown
     - ✅ If profile missing: fallback label shown (e.g., "Pro User #123" or role-based)
     - ✅ Label is not generic ("Unknown User", "User", etc.)

2. Open chat view:
   - Verify chat header shows counterpart identity (same fallback logic)
   - Check identity is stable across multiple messages

3. Document results in VERIFICATION-REPORT.md under MSGS-01, MSGS-02

**Acceptance:**
- All conversations show appropriate identity labels
- No generic labels in production
- Fallback logic works as specified
</task>

<task id="7.1.10">
**Execute UAT: Dashboard rating card (DASH-01)**

1. In staging environment:
   - Navigate to user dashboard
   - View rating card:
     - ✅ Average rating displayed (e.g., "4.5")
     - ✅ Formatting is consistent with spec (1 decimal place, or as specified)
     - ✅ If no ratings: "No ratings yet" or similar shown
     - ✅ If new ratings added: card updates

2. Test with different rating profiles:
   - High-rated user (4.5+)
   - Medium-rated user (2-4)
   - Newly rated user (few ratings)
   - No ratings

3. Document results in VERIFICATION-REPORT.md under DASH-01

**Acceptance:**
- Rating card accuracy verified
- All edge cases work
- Formatting is correct
</task>

<task id="7.1.11">
**Execute UAT: Rating breakdown popup (DASH-02)**

1. In staging environment:
   - Click on rating card
   - Verify popup/modal opens
   - Check breakdown display:
     - ✅ 5★ through 1★ rows visible
     - ✅ Counts accurate (matches total reviews)
     - ✅ Percentages sum to ~100% (allow rounding tolerance)
     - ✅ Visual display is clear (bar chart, percentage labels, etc.)

2. Close and reopen:
   - ✅ Popup closes on close button
   - ✅ Can reopen by clicking rating card again

3. Document results in VERIFICATION-REPORT.md under DASH-02

**Acceptance:**
- Breakdown popup works correctly
- Math is accurate (percentages sum to 100%)
- UI is clear and functional
</task>

<task id="7.1.12">
**Review and sign off**

1. Complete VERIFICATION-REPORT.md:
   - All 4 requirements have test results and UAT findings
   - No open issues blocking acceptance
   - All tests passed

2. Sign off:
   - Update YAML: `verified_on: 2026-04-04`
   - Update `verification_status: complete`
   - Add QA sign-off: `qa_sign_off: verified_by_claude_code_2026-04-04`

3. Commit report

**Acceptance:**
- Report reviewed and signed off
- Ready for milestone re-audit
</task>

## Verification Checkpoints

- [ ] VERIFICATION-REPORT.md created in `.planning/phases/04-messaging-dashboard/`
- [ ] MSGS-01 (conversation list identity) UAT results recorded
- [ ] MSGS-02 (chat header identity) UAT results recorded
- [ ] DASH-01 (rating card average) UAT results recorded
- [ ] DASH-02 (star breakdown) UAT results recorded
- [ ] All unit tests pass (>80% coverage per requirement)
- [ ] Integration test passes
- [ ] Report reviewed and signed off

## Dependencies

- None (can execute independently)

## Definition of Done

- VERIFICATION-REPORT.md committed to git
- All 4 requirements marked as satisfied in report
- Phase 4 is fully verified
- Ready for re-audit: `/gsd-audit-milestone` will show MSGS-01, MSGS-02, DASH-01, DASH-02 as "satisfied"
